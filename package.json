{
  "name": "grain-catcher",
  "version": "0.1.0",
  "description": "Cloudflare Worker that turns Grain meeting webhooks into OmniFocus tasks.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mimetext": "^3.0.24"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.95.0"
  }
}
